<h2>Edit photo details</h2>

<%= link_to image_tag(@photo.photo.url(:thumb)),
 											album_photo_path(:album_id => @photo.album, :id => @photo) %>

<%= render :partial => "form" %>

<%= link_to 'Show', album_photo_path(@album,@photo) %> |
<%= link_to 'Back', album_photos_path(@album) %>
